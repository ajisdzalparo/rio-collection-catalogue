// Prisma Schema for RIO Collection Catalogue & Backend API

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  name      String
  email     String   @unique
  role      String   @default("Admin")
  status    String   @default("active")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Role {
  id          String   @id @default(cuid())
  name        String   @unique
  permissions Json
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Category {
  id          String    @id @default(cuid())
  name        String
  slug        String    @unique
  description String?
  isActive    Boolean   @default(true)
  deletedAt   DateTime?
  createdAt   DateTime  @default(now())
}

model Color {
  id        String    @id @default(cuid())
  name      String
  hex       String
  isActive  Boolean   @default(true)
  deletedAt DateTime?
  createdAt DateTime  @default(now())
}

model Size {
  id        String    @id @default(cuid())
  size      String    @unique
  isActive  Boolean   @default(true)
  deletedAt DateTime?
  createdAt DateTime  @default(now())
}

model Product {
  id               String           @id @default(cuid())
  name             String
  slug             String           @unique
  edition          String           @default("Edition 001")
  category         String
  color            String
  colorHex         String
  colors           String[]
  colorHexes       String[]
  price            Int
  hpp              Int?
  stock            Int              @default(0)
  stockMode        String           @default("QUANTITY") // QUANTITY, ALWAYS_AVAILABLE
  status           String           @default("AVAILABLE") // AVAILABLE, SOLD_OUT, COMING_SOON
  imageUrl         String
  images           String[]
  imageDetails     Json?
  description      String
  storyTitle       String?
  storyText        String?
  materialsAndCare Json?
  variants         ProductVariant[]
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt
}

model ProductVariant {
  id        String  @id @default(cuid())
  productId String
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  size      String
  inStock   Boolean @default(true)
  stock     Int     @default(0)
}

model Archive {
  id          String   @id @default(cuid())
  name        String
  slug        String   @unique
  status      String   @default("SOLD_OUT")
  imageUrl    String
  description String
  createdAt   DateTime @default(now())
}

model Journal {
  id                 String   @id @default(cuid())
  title              String
  slug               String   @unique
  author             String
  date               String
  category           String
  imageUrl           String
  excerpt            String
  content            String[]
  pullQuote          String?
  relatedProductSlug String?
  createdAt          DateTime @default(now())
}

model Topic {
  id          String    @id @default(cuid())
  name        String
  description String
  isActive    Boolean   @default(true)
  deletedAt   DateTime?
  createdAt   DateTime  @default(now())
}

model Testimony {
  id        String   @id @default(cuid())
  alt       String
  imageUrl  String
  status    String   @default("ACTIVE")
  createdAt DateTime @default(now())
}

model Order {
  id             String      @id @default(cuid())
  orderNumber    String      @unique
  fullName       String
  whatsapp       String
  address        String
  notes          String?
  adminNotes     String?
  courierName                    String?
  trackingNumber                 String?
  quotedShippingFee              Int         @default(15000)
  shippingFee                    Int         @default(15000)
  shippingAdjustmentAmount       Int         @default(0)
  shippingAdjustmentStatus       String      @default("NONE") // NONE, CUSTOMER_CONFIRMATION_PENDING, CUSTOMER_CONFIRMED, REFUNDED, REFUND_WAIVED
  shippingAdjustmentNote         String?
  shippingAdjustmentWaSent      Boolean     @default(false)
  paymentProofUrl                String?
  additionalPaymentProofUrl      String?
  refundProofUrl                 String?
  shippingProofUrl               String?
  totalPrice                     Int
  status                         String      @default("PENDING") // PENDING, PAID, FULFILLED, CANCELLED
  waFollowedUp                   Boolean     @default(false)
  items                          OrderItem[]
  createdAt      DateTime    @default(now())
  updatedAt      DateTime    @updatedAt
}

model OrderItem {
  id        String   @id @default(cuid())
  orderId   String
  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  productId String?
  name      String
  size      String
  price     Int
  quantity  Int
  createdAt DateTime @default(now())
}

model StoreSettings {
  id                String   @id @default("default")
  storeName         String   @default("RIO COLLECTION")
  whatsappNumber    String   @default("628123456789")
  bankName          String?  @default("BCA")
  bankAccountNumber String?  @default("1234567890")
  bankAccountOwner  String?  @default("RIO COLLECTION")
  flatShippingRate  Int      @default(15000)
  instagramUrl      String?
  tiktokUrl         String?
  facebookUrl       String?
  pinterestUrl      String?
  xTwitterUrl       String?
  heroTitle         String?  @default("EDITION 001")
  heroSubtitle      String?  @default("ARCHIVAL COTTON SILHOUETTE")
  heroLeftImage     String?  @default("https://images.unsplash.com/photo-1503342217505-b0a15ec3261c?w=1200&auto=format&fit=crop&q=80")
  heroRightImage    String?  @default("https://images.unsplash.com/photo-1523381210434-271e8be1f52b?w=1200&auto=format&fit=crop&q=80")
  heroCtaText       String?  @default("Eksplor Koleksi Terkini")
  heroCtaLink       String?  @default("/catalogue")
  homeFeaturedTitle String?
  homeViewAllLabel  String?
  homeManifestoTitle String?
  homeManifestoText String?
  homeManifestoImage String?
  homeBannerText    String?
  homeBannerButton  String?
  archiveHeaderSub  String?
  archiveQuoteTitle String?
  archiveQuoteText  String?
  aboutHeroImage    String?
  aboutHeading      String?
  aboutParagraph1   String?
  aboutParagraph2   String?
  aboutValuesTitle  String?
  aboutValues       Json?
  aboutQuote        String?
  aboutQuoteText    String?
  aboutStudioImage  String?
  contactEmail      String?
  waTemplatePending  String?  @default("Halo {nama_pelanggan},\n\nTerima kasih telah memesan dari RIO COLLECTION!\nKami telah menerima pesanan Anda (#{nomor_order}).\n\nTotal Tagihan: {total_pembayaran}\nSilakan melakukan pembayaran via transfer bank:\n{rekening_bank}\n\nHarap kirimkan bukti transfer ke WhatsApp ini setelah melakukan pembayaran. Terima kasih!")
  waTemplatePayment  String?  @default("Halo {nama_pelanggan},\n\nKami dari RIO COLLECTION mengonfirmasi bahwa pembayaran untuk pesanan #{nomor_order} sebesar {total_pembayaran} telah DITERIMA & LUNAS.\n\nPesanan Anda sedang kami siapkan untuk pengiriman. Terima kasih!")
  waTemplateShipping String?  @default("Halo {nama_pelanggan},\n\nPesanan Anda (#{nomor_order}) dari RIO COLLECTION telah dikirim via {kurir}!\nNomor Resi: {nomor_resi}\n\nTerima kasih telah berbelanja di RIO COLLECTION!")
  waTemplateRemind   String?  @default("Halo {nama_pelanggan},\n\nKami dari RIO COLLECTION menginformasikan bahwa pesanan (#{nomor_order}) dengan total {total_pembayaran} sedang menunggu pembayaran.\n\nApakah ada kendala saat melakukan proses pembayaran?\nMohon konfirmasi/kirimkan bukti bayar agar pesanan dapat segera dikirim. Terima kasih!")
  enabledCouriers    String?  @default("jne,pos,tiki,sicepat,jnt")
  originCityId       String?  @default("153")
  originCityName     String?  @default("Kota Bandung")
  originProvinceName String?  @default("Jawa Barat")
  updatedAt         DateTime @updatedAt
}

/// Master bank — daftar bank yang tersedia untuk dipilih toko
model Bank {
  id        String   @id @default(cuid())
  name      String   @unique
  code      String?  // e.g. BCA, BNI, MANDIRI
  logoUrl   String?
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
}

/// Rekening bank toko — bisa lebih dari satu, client hanya melihat yang isActive
model StoreBank {
  id                String   @id @default(cuid())
  bankName          String
  accountNumber     String
  accountOwner      String
  isActive          Boolean  @default(true)
  sortOrder         Int      @default(0)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}

/// Cache tarif ongkir lokal agar hemat API limit RajaOngkir dan respon super cepat (0ms)
model ShippingRateCache {
  id                 String   @id @default(cuid())
  originCityId       String
  destinationCityId  String
  courier            String
  weightGrams        Int      @default(1000)
  ratesData          Json
  expiresAt          DateTime
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@unique([originCityId, destinationCityId, courier, weightGrams])
  @@index([destinationCityId, courier])
}
